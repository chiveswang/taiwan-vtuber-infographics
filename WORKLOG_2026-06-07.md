# 2026-06-07 Worklog

## Completed

- Finished staged stream extraction through S4 (`--streams-weekdays all`).
  - YouTube: 1,460 daily representative snapshots, 362,009 cleaned streams.
  - Twitch: 1,072 daily representative snapshots, 1,851 cleaned streams.
  - `streaming.json` now includes full 7/7 coverage for both `yt` and `tw`.
- Added Twitch longitudinal followers to `build_longitudinal.py`.
  - `growth.json` now includes `series_tw`, `tw_now`, `tw_cagr_yr`, `tw_mom_3m`, `tw_peak`, and `tw_drawdown`.
  - Added `crossplatform.json` for dual-platform percentile comparison and platform tilt.
- Expanded dashboard in `build_full_dashboard.py`.
  - Added `s_rank` leaderboard page.
  - Added `s_agency` group/agency ecosystem page.
  - Added `s_report` generated industry report page.
  - Expanded `s_growth` with Twitch trajectory and `gw_cross` dual-platform comparison.
  - Upgraded `s_locate` with channel health card and group/indie comparison.
- Fixed stickiness ranking for zero recent views.
  - `r=0` is now valid data, not missing.
  - Percentile now uses strict "wins over" semantics, so zero stickiness ranks at 0.0%.
  - Rank now counts channels strictly above the value, preventing zero values from displaying as `n+1/n`.
- Added dashboard avatar integration.
  - `build_locate_directory.py` now stores YouTube/Twitch thumbnail URL as `img`.
  - `build_full_dashboard.py` renders avatars in similar-channel results, leaderboards, and agency member tables with lazy loading and broken-image fallback.
- Added first live-presentation visual polish pass.
  - Leaderboards now render a horizontal bar chart above the clickable table.
  - Dual-platform comparison now includes a YouTube/Twitch percentile quadrant scatter plot.
- Added report-page live presentation cards.
  - `s_report` now opens with large story cards for tracked VTubers, dual-platform count, concentration pressure, content theme, and streaming peak hour.
- Added agency tier visualization.
  - `s_agency` now includes a scatter plot with total subscribers, per-member median subscribers, and member-count point size.
- Added slide-like report chapters.
  - `s_report` now has four chapter cards for market scale, platform split, concentration vs long tail, and schedule-proxy caveat.
- Added live briefing deck generator.
  - `build_presentation_deck.mjs` creates a 10-slide editable PPTX draft from existing generated dashboard JSON.
  - Output is `outputs/activity/taiwan-vtuber-live-briefing.pptx` and remains ignored.
  - Second pass added a data-scope/safety-boundary slide and removed a hard-coded agency tier count.

## Verified

- `python build_longitudinal.py --streams-weekdays all`
- `python build_full_dashboard.py`
- JavaScript parse check: `js-parse-ok`
- `streaming.json` coverage is 7/7 for YouTube and Twitch.
- `growth.json` includes Twitch fields; `crossplatform.json` is generated and embedded.
- `locate_directory.json`: 3,033 channels, 3,033 with `img`.
- Renee Snowy zero-value check: recent heat `0` ranks `1669/2686`; stickiness `0.0` ranks `1669/2571`, percentile `0.0%`.
- Dashboard static checks: `rankBar`, `gwX4`, and dual-platform quadrant markup embedded.
- `crossplatform.json`: 1,945 dual-platform scatter points; 102 are top-right 90/90 percentile channels.
- Dashboard static checks: report `story-grid/story-card` markup embedded; JavaScript parse remains OK.
- Dashboard static checks: `agTier` agency tier chart embedded; JavaScript parse remains OK.
- Agency tier chart: 235 plotted groups from 378 group names.
- Dashboard static checks: `chapter-grid` and chapter copy embedded; JavaScript parse remains OK.
- Presentation check: generated PPTX imports with artifact-tool and contains 10 slides; zip structure check also reports 10 slides.

## Data Safety Notes

- Large local data remains ignored: `archive/`, `current/`, `outputs/`, `quarterly_samples/`, SQLite files, and `outputs/longitudinal/`.
- Final JSON/HTML outputs are generated under ignored `outputs/`.
- Versioned changes are limited to scripts and documentation.

## Remaining / To Confirm

- Cross-platform dual-channel count is currently 1,945 using the definition "has YouTube subscribers and Twitch followers"; the earlier task estimate was 1,525. Confirm whether a stricter dual-platform definition is needed.
- Visual polish remains possible for live presentation: stronger final deck art direction and screenshot/native chart polish.

## 01:40 Closeout Check

- SQLite baseline still queryable:
  - archive: 83,322 files / 83,314 CSV.
  - current: 13,232 files / 13,226 CSV.
  - current latest_rows: 7,471.
- Regenerated dashboard:
  - `python build_full_dashboard.py`
  - JavaScript parse: `js-parse-ok`
  - Static markers present: `rankBar`, `gwX4`, `agTier`, `story-grid`, `chapter-grid`.
- Regenerated live briefing PPTX:
  - `node build_presentation_deck.mjs`
  - artifact-tool import: 10 slides.
  - Output remains `outputs/activity/taiwan-vtuber-live-briefing.pptx` and ignored.
- Data safety closeout:
  - `git ls-files` has no `archive/`, `current/`, `outputs/`, `quarterly_samples/`, SQLite, or PPTX paths.
  - Secret keyword scan over versioned workspace paths returned no matches.
  - Working tree after latest commit should contain only ignored local data.

## Concentration Follow-up

- Completed five dashboard tickets:
  - Added YouTube view-count top-10 concentration alongside subscriber concentration.
  - Fixed rank leaderboard rendering and empty-board handling.
  - Kept the dual-platform scatter visible when a channel is selected and added selected-channel highlighting.
  - Synced located channel state into growth schedule view and added a clear no-stream-sample note.
  - Replaced user-visible developer/data-pipeline wording with audience-facing report language.
- Expanded concentration analysis:
  - Added monthly, quarterly, and yearly concentration datasets from `basic-data`.
  - Added `累計觀看` and `期間觀看數` terminology throughout the visible dashboard.
  - Added the concentration page controls for time grain, period, metric, threshold, and specific channel.
  - Added `存量 vs 本期流量：特異值` with subscriber percentile vs period-view percentile scatter.
  - Added tables for `沉睡大台`, `流量黑馬`, and `注意力超配`.

## Concentration Verification

- `python analyze_quarterly_full.py`
- `python build_full_dashboard.py`
- `python -m py_compile analyze_quarterly_full.py build_full_dashboard.py`
- `node --check outputs/activity/_dashboard_check.js`
- Browser smoke through local inline-check page:
  - concentration controls rendered for month/quarter/year and `期間觀看數`.
  - outlier card DOM rendered with `ccOutlier`, `沉睡大台`, `流量黑馬`, and `注意力超配`.
  - no browser console errors were observed in the checked flow.
- Data safety:
  - `git ls-files` still has no `archive/`, `current/`, `outputs/`, `quarterly_samples/`, SQLite, or PPTX paths.

## Outlier Active Filter Fix

- Added channel Activity status into the concentration dataset.
- Updated the `存量 vs 本期流量：特異值` panel to only include `Activity=Active` channels.
- Added visible copy explaining that graduated and inactive channels are excluded from sleeping/black-horse judgments.
- Rebuilt the dashboard and verified the sleeping-large-channel candidate set contains no Graduated rows.

## Outlier Zero-Value Triage

- Changed the main sleeping-large-channel outlier ranking to use a rolling recent window: current period plus previous two periods.
- Kept the main sleeping/black-horse/attention-overfit lists on non-zero rolling period views so zero values do not dominate the primary analysis.
- Added separate zero-value triage tables:
  - `零期間觀看檢查：訂閱高但近3期為 0`
  - `資料疑似缺口：累計觀看也為 0`
  - `停更候選：累計觀看存在但近3期為 0`
- Verified the main sleeping-large-channel list has no zero rolling-period-view rows, while high-subscription zero rows are still visible in the check list.

## Industry Report Concentration Sync

- Updated the generated industry report page to match the new concentration analysis.
- The report now separates subscriber concentration, cumulative-view concentration, and rolling period-view interpretation.
- Added a report-level `近3期零值` story card and concentration paragraph that summarizes high-subscription Active channels with zero rolling period views.
- Rebuilt the dashboard and verified the generated HTML contains the new concentration/zero-triage report copy.
